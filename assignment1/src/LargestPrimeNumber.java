public class LargestPrimeNumber {

	public static void main(String[] args) {
		LargestPrimeNumber lpn = new LargestPrimeNumber();
		int a = lpn.prime(20);
		System.out.println(" Largest Prime number smaller than 20 is " + a);
	}
	
	/**
	 * Used to find the largest prime number
	 * smaller than the passed in number
	 * @param a
	 * @return
	 */
	private int prime(int a) {
		//TODO Actual logic here
		return 0;
	}
}
